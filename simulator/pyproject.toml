[project]
name = "simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rahul Kumar", email = "prathamskt@gmail.com" }
]
requires-python = ">=3.14,<3.15"
dependencies = [
    "paho-mqtt>=2.1.0",
]

[project.scripts]
simulator = "simulator:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
